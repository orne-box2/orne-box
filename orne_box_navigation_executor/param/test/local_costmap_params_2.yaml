local_costmap:
  global_frame: odom           #大事そうだけど変化なし mapを推奨されている
  #robot_base_frame: base_link   # defaultと一緒
  update_frequency: 2.3       # 2.5
  publish_frequency: 2.0        # 関係ない rvizでみたいからやっぱいる
  #static_map: false           
  rolling_window: true          # trueじゃないと動かない
  #resolution: 0.05            
  #width: 8.0                    
 # height: 8.0                  
  transform_tolerance: 0.3     
  inflation_radius: 0.4    
 
